source:
  type: mysql
  config:
    platform_instance: CMS.OCE.PROD
    env: PROD
    host_port: sl73piedbp001.visa.com:3306
    #    database: dbname
    username: ${CMS_PIE_OCE_PROD_USERNAME}
    password: ${CMS_PIE_OCE_PROD_PASSWORD}
    stateful_ingestion:
      enabled: true
      fail_safe_threshold: 100
    schema_pattern:
      allow:
        - "^pie$"
#      deny:
#        -
#    table_pattern:
#      deny:
#        -
#sink:
#  type: file
#  config:
#    filename: cms-oce-prod-ingestion.json
pipeline_name: "CMS OCE PROD"
sink:
  type: datahub-rest
  config:
    server: '${DATAHUB_GMS_HOST}'
    token: '${DATAHUB_GMS_TOKEN}'
    max_threads: 1
