source:
  type: mysql
  config:
    platform_instance: VTRUST.OCC.PROD
    env: PROD
    host_port: SL55VTPSDBP001.visa.com:3306
    #    database: dbname
    username: ${VTRUST_USERNAME}
    password: ${VTRUST_PASSWORD}
    stateful_ingestion:
      enabled: true
      fail_safe_threshold: 100
    schema_pattern:
      allow:
        - vtrust
#      deny:
#        -
#    table_pattern:
#      deny:
#        -
#sink:
#  type: file
#  config:
#    filename: vtrust-occ-prod-ingestion.json
pipeline_name: "VTRUST OCC PROD"
sink:
  type: datahub-rest
  config:
    server: '${DATAHUB_GMS_HOST}'
    token: '${DATAHUB_GMS_TOKEN}'
    max_threads: 1
