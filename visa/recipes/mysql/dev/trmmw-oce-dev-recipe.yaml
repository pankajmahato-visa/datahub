source:
  type: mysql
  config:
    platform_instance: TRMMW.OCE.DEV
    env: DEV
    host_port: sl73trmmwdbd001.visa.com:3306
    #    database: dbname
    username: ${TRMMW_USERNAME}
    password: ${TRMMW_PASSWORD}
    stateful_ingestion:
      enabled: true
      fail_safe_threshold: 100
    schema_pattern:
      #      allow:
      #        -
      deny:
        - performance_schema
#    table_pattern:
#      deny:
#        -
#sink:
#  type: file
#  config:
#    filename: trmmw-oce-dev-ingestion.json
pipeline_name: "TRMMW DEV"
sink:
  type: datahub-rest
  config:
    server: '${DATAHUB_GMS_HOST}'
    token: '${DATAHUB_GMS_TOKEN}'
    max_threads: 1
