source:
  type: hive
  config:
    platform_instance: OPR.OCE.DEV
    env: DEV
    host_port: 'sl73caehmnc606.visa.com:10000'
    username: svcdlcnifi
    options:
      connect_args:
        auth: KERBEROS
        kerberos_service_name: hive
    stateful_ingestion:
      enabled: false
#    schema_pattern:
#      allow:
#        -
#      deny:
#        -
#    table_pattern:
#      deny:
#
sink:
  type: datahub-rest
  config:
    server: '${DATAHUB_GMS_HOST}'
    token: '${DATAHUB_GMS_TOKEN}'
    max_threads: 1
