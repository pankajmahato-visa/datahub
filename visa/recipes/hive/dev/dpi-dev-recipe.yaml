source:
  type: hive
  config:
    platform_instance: DPI.OCE.DEV
    env: DEV
    host_port: 'sl73dpihmnu0107.visa.com:10000'
    username: svcdpidlcd
    options:
      connect_args:
        auth: KERBEROS
        kerberos_service_name: hive
#    schema_pattern:
#      allow:
#        -
#      deny:
#        -
#    table_pattern:
#      deny:
#
sink:
  type: datahub-rest
  config:
    server: '${DATAHUB_GMS_HOST}'
    token: '${DATAHUB_GMS_TOKEN}'
    max_threads: 1